# ZHANDI 仿《战地5》体验分阶段优化计划

> 文档版本：v1.0  
> 制定日期：2026-08-03  
> 适用项目：`zf-frontend`  
> 技术栈：TypeScript 6 + Three.js + Rapier3D + WebSocket + Vite

## 1. 项目目标与边界

### 1.1 总目标

把当前“功能覆盖较广、表现偏原型”的 Web FPS，优化为一个具备《战地5》核心感受的可持续开发版本。优先还原的不是商业作品的资产数量，而是以下体验支柱：

1. **大规模战场感**：稳定的征服模式、明确的前线变化、友军与敌军共同推进。
2. **扎实枪械手感**：每类武器拥有可辨识的后坐力、弹道、音效和动画反馈。
3. **步兵与载具协同**：兵种职责清楚，坦克、运输与反载具形成克制关系。
4. **动态战场氛围**：烟尘、爆炸、天气、环境音和局部破坏共同塑造压迫感。
5. **可靠的多人对战基础**：服务器权威、状态同步、延迟补偿和断线恢复可验证。
6. **浏览器可承受的性能**：中端桌面设备 1080p 可稳定游玩，并具备可降级画质。

### 1.2 法务与内容边界

- 只借鉴玩法结构、交互逻辑与战场氛围，不直接复制《战地5》的商标、UI 图标、角色、地图、音乐、语音、模型或纹理。
- 武器与二战装备可依据历史资料制作原创资产；所有第三方资源必须记录来源与许可证。
- 对外发布时使用项目自有名称 `ZHANDI`、原创阵营标识与原创视觉语言。

### 1.3 本轮不优先实现

- 64 人正式商用服务器、全球匹配、付费系统、赛季通行证。
- 全地图任意建筑完全破坏。
- 对商业 3A 资产精度的一比一复制。
- 飞机系统、复杂海战、跨平台账号体系。

先做可玩的 **16v16 征服垂直切片**，再评估扩展到 32v32。

---

## 2. 当前基线审计

### 2.1 已有能力

| 系统 | 当前实现 | 关键文件 | 成熟度判断 |
|---|---|---|---|
| 游戏入口与主循环 | 菜单进入后初始化全部系统，单一 RAF 主循环 | `src/main.ts`、`src/game/GameScene.ts` | 可运行，耦合过高 |
| 玩家控制 | 行走、冲刺、跳跃、蹲伏、体力、FOV、头部摆动、后坐力、坠落伤害 | `src/player/PlayerController.ts` | 原型可玩 |
| 枪械 | STG44、Suomi、MG42、Kar98k；射速、弹匣、换弹、伤害、散布、射线命中 | `src/weapons/*` | 有框架，缺深度和动画 |
| AI | 两阵营共 8 个 Bot，巡逻、追击、攻击、找掩体、重生 | `src/ai/AIBot.ts` | 功能丰富，缺导航与小队战术 |
| 征服玩法 | A/B/C 三点、占领、兵力值流失、胜负条件 | `src/game/ConquestMode.ts` | 基本闭环 |
| 地图 | 120×120 柏林废墟程序化方块建筑、道路、瓦砾 | `src/maps/BerlinRuins.ts` | 灰盒级 |
| 载具 | 吉普、坦克等配置；项目当前生成两辆吉普，可进入和驾驶 | `src/vehicle/VehicleSystem.ts` | 原型级，载具武器无伤害闭环 |
| 战术装备 | 破片、烟雾、闪光弹及效果 | `src/equipment/TacticalEquipment.ts` | 原型级 |
| 天气与昼夜 | 晴、雨、雪、雾、沙尘；粒子与光照变化 | `src/environment/WeatherSystem.ts` | 可演示，缺材质联动 |
| 音频 | WebAudio 合成枪声、脚步、爆炸、命中等，支持 HRTF | `src/audio/AudioSystem.ts` | 占位音频 |
| HUD/UI | 菜单、设置、计分板、准星、血量、弹药、小地图、征服兵力值 | `src/ui/*` | 信息齐全，视觉偏原型 |
| 网络 | WebSocket 位置广播，客户端文件中存在预测/插值/反作弊类 | `src/network/*` | 非权威原型，若干能力未真正接入 |
| 性能 | 性能预算、监控、LOD/实例化类、Vite 手动分包 | `src/performance/*` | 工具存在但未形成闭环 |

### 2.2 已验证基线

- `npm run build` 成功。
- 构建产物中业务包约 **124.72 kB**，Three.js 约 **530.90 kB**，Rapier 约 **2,235.32 kB**（均为压缩前），Rapier gzip 后约 **829.84 kB**。
- 当前无项目自有自动化测试。
- `GameScene.ts` 约 1,600 行，负责初始化、玩法、AI、射击、载具、特效、网络、HUD 和主循环，是首要技术风险。

### 2.3 主要缺口

1. **宣传完成度高于真实完成度**：README 标记“多人同步、预测、服务器验证、反作弊”等已完成，但实际在线链路主要是位置广播。
2. **非固定物理步长**：`PhysicsWorld.step(dt)` 未使用传入 `dt`，物理行为可能随帧率变化。
3. **碰撞不完整**：玩家主要以高度判断落地，灰盒地图视觉物体与 Rapier 世界未形成完整一致的碰撞/导航数据。
4. **主循环耦合**：任何功能扩展都容易引起回归，难以单测、压测和多人复用。
5. **远端玩家直接瞬移**：当前收到更新后直接设置位置，没有可用的快照缓冲与插值。
6. **服务器非权威**：服务器不管理房间、队伍、生命、武器、命中、载具、据点和胜负，无法保障一致性。
7. **地图与角色均为几何体**：没有 GLTF 角色、武器、载具、PBR 材质、骨骼动画或环境资产管线。
8. **音频为程序合成占位**：无法提供接近战争题材所需的层次、距离感、室内外差异和冲击力。
9. **性能优化类存在逻辑风险**：现有 LOD/实例化实现可能错误移除父子关系或生成 100 个未设置矩阵的实例，不能直接用于生产。
10. **缺少质量门禁**：没有 lint、测试、性能回归场景、网络模拟、错误上报与版本化配置。

---

## 3. 总体推进原则

### 3.1 优先级顺序

`稳定性与架构 → 垂直切片 → 枪械/移动手感 → 征服与AI → 资产/画面/音频 → 载具/破坏 → 权威联网 → 性能压测 → 发布`

### 3.2 阶段准入规则

- 上一阶段的 P0 验收项全部通过，才能进入下一阶段。
- P1 问题可带入下一阶段，但必须登记负责人和处理阶段。
- 每阶段都必须保留：可运行构建、性能采样、回归清单、变更日志。
- 新功能必须配套调试开关，不允许只能靠肉眼猜测状态。

### 3.3 目标平台与性能档位

| 档位 | 参考设备 | 分辨率 | 目标 |
|---|---|---:|---:|
| 低 | 集显/入门独显、8 GB 内存 | 1280×720 | P95 帧时间 ≤ 33.3 ms，30 FPS 稳定 |
| 中 | GTX 1660 / RX 580 级别 | 1920×1080 | P95 帧时间 ≤ 16.7 ms，60 FPS 稳定 |
| 高 | RTX 3060 / RX 6600 及以上 | 1920×1080 | P95 帧时间 ≤ 13.9 ms，画质优先 |

首屏进入菜单目标：缓存后 ≤ 2 秒；首次进入战局目标：普通宽带下 ≤ 8 秒；长任务不能阻塞主线程超过 50 ms。

---

# 4. 分阶段实施计划

## 阶段 0：冻结基线与建立质量门禁

**目标：** 先把“能运行”变成“能量化、能回归、能安全修改”。  
**建议周期：** 3–5 个工作日。

### 任务

#### P0

- 建立 `GameConfig`，集中管理移动、枪械、AI、网络、性能参数，消除散落常量。
- 增加 ESLint、Prettier、Vitest；构建命令拆为 `typecheck`、`test`、`build`。
- 增加冒烟测试：菜单打开、离线战局启动、玩家生成、武器射击、占点更新、死亡重生。
- 在 HUD 调试层显示 FPS、P50/P95 帧时间、draw calls、三角形、纹理数、实体数。
- 创建固定的基准战场：固定随机种子、8 Bot、2 载具、固定天气，避免每次测试场景不同。
- 更新 README 的真实完成状态，区分“已接入”“仅有类定义”“待生产化”。

#### P1

- 为游戏配置增加 schema 校验和版本号。
- 增加开发环境错误覆盖层与结构化日志级别。
- 建立浏览器兼容矩阵：Chrome/Edge 主支持，Firefox 次支持，Safari 作为后续兼容目标。

### 验收标准

- `typecheck + test + build` 一键通过。
- 连续启动/退出战局 10 次，无重复事件监听、无明显 GPU 资源增长。
- 基准战场连续运行 15 分钟无崩溃。
- 性能指标可导出为 JSON，并保留一次基线数据。

### 阶段退出物

- 质量门禁脚本、基准场景、性能基线报告、真实状态 README。

### 执行记录（2026-08-03）

- [x] 建立版本化 `GameConfig` 与运行时校验，关键玩家、武器、AI、网络和性能参数已开始集中管理。
- [x] 加入 ESLint、Prettier、Vitest/jsdom，以及 `typecheck`、`test`、`build:check`、`check` 脚本。
- [x] 建立 7 个测试文件、15 项自动化测试，覆盖配置、固定随机源、输入生命周期、武器、生命/重生、征服和性能统计。
- [x] 加入 `?benchmark=1` 固定基准模式：固定种子、8 Bot、2 辆吉普和固定天气。
- [x] 加入 F3 性能面板，展示 FPS、P50/P95、draw calls、三角形、纹理/几何体和实体数，并支持导出 JSON。
- [x] README 已改为“已接入/原型级/仅有代码”的真实状态矩阵和浏览器支持矩阵。
- [x] `npm run check` 已通过，包含格式、ESLint、类型、15 项测试和隔离构建。
- [ ] 连续启动/退出 10 次与 15 分钟基准长稳测试仍需浏览器人工运行；完成后导出首份真实性能基线 JSON。
- [ ] 开发错误覆盖层与结构化日志留待阶段 1 生命周期重构时接入。

---

## 阶段 1：架构解耦与确定性主循环

**目标：** 解决 1,600 行 `GameScene` 单体类，为后续扩展和联网打基础。  
**建议周期：** 1–2 周。

### 任务

#### P0

- 把 `GameScene` 拆为以下层次：
  - `GameApp`：生命周期、菜单、场景切换。
  - `World`：实体注册、查询、销毁。
  - `Simulation`：固定步长玩法和物理更新。
  - `RenderSystem`：插值渲染、相机、特效。
  - `CombatSystem`、`ConquestSystem`、`VehicleSystem`、`AISystem`、`UISystem`。
- 使用固定逻辑步长：模拟 60 Hz，渲染按屏幕刷新率运行，累计器限制追帧次数。
- 统一时间来源，玩法层不再混用 `performance.now()`、`Date.now()` 和 `setTimeout()`。
- 建立明确的游戏状态机：`BOOT → MENU → LOADING → PLAYING → PAUSED → ROUND_END → DISPOSED`。
- 统一资源所有权和 `dispose()`，处理材质、纹理、几何体、CanvasTexture、音频节点和事件监听器。
- 将命中、击杀、占点、爆炸等通信改为类型化事件总线，避免模块互相直接调用内部状态。

#### P1

- 引入轻量 ECS 思路，但不强制使用第三方 ECS：实体 ID + 组件数据 + 系统查询即可。
- 将浏览器 DOM UI 和战斗模拟彻底隔离。
- 增加暂停、失焦、页面隐藏时的时间处理，避免回到页面后物理爆炸。

### 验收标准

- `GameScene` 仅保留编排职责，建议控制在 300 行以内。
- 30/60/120 FPS 下移动 10 秒的距离差异 < 2%。
- 同一输入录制回放 3 次，核心战斗结果一致。
- 每个核心系统可以独立构造并执行单元测试。

### 关键风险

- 不能“大爆炸式重写”。按主循环、战斗、AI、UI 顺序渐进迁移，每次迁移后保持可玩。

### 增量执行记录（2026-08-03）

- [x] 新增独立 `FixedStepClock`，模拟固定为 60 Hz，渲染继续跟随 RAF；长帧限制为 100 ms，单帧最多追赶 5 个模拟步。
- [x] 主循环已拆出 `simulateFixedStep()`，物理、玩家、武器、AI、天气、征服和 HUD 逻辑按固定步长推进。
- [x] Rapier `world.timestep` 显式使用固定 `dt`，不再忽略传入时间步。
- [x] 新增 `GameStateMachine`，接入 `BOOT → MENU → LOADING → PLAYING → PAUSED → ROUND_END → DISPOSED` 生命周期。
- [x] Esc 暂停、页面隐藏和恢复均重置累计器，避免恢复页面后集中追帧。
- [x] 核心玩法回调统一使用 `simulationTimeMs`；网络发送仍使用协议需要的真实时间戳。
- [x] 自动化测试增至 10 个文件、23 项测试，覆盖固定时钟、状态转换和 Rapier timestep。
- [x] `npm run check` 已再次通过，开发基准地址可访问。
- [x] 新增类型化 `EventBus<GameEvents>`，击杀、玩家死亡、回合结束和 UI 消息已通过事件分发。
- [x] 新增 `ConquestPresenter`，占点实体收集、征服更新和 HUD 快照已可独立测试。
- [x] 新增输入录制/序列化/回放基础设施，并通过 30/60/120 FPS 一秒模拟一致性测试。
- [x] 跨帧率测试发现并修复固定时钟浮点边界少执行一步的问题。
- [x] 自动化覆盖增至 13 个文件、32 项测试，完整质量门禁通过。
- [ ] `GameScene` 仍是大型编排类；Combat/Vehicle/AI/UI 系统将继续按阶段渐进抽离，不进行高风险一次性重写。
- [ ] 基于真实 PlayerController 的 10 秒跨帧率移动误差还需要完成可注入物理适配后验证。

---

## 阶段 2：步兵移动与枪械手感垂直切片

**目标：** 先把 1 个兵种 + 2 把武器做到“愿意反复玩”，再扩展内容。  
**建议周期：** 2 周。

### 任务

#### P0：移动

- 使用 Rapier shape cast/raycast 正确判断地面、坡度、台阶、头顶阻挡。
- 增加蹲姿碰撞体切换、矮墙翻越、攀爬、滑铲或战术冲刺中的至少两项。
- 增加地面加速/减速、空中控制、斜坡限速、落地缓冲和镜头惯性曲线。
- 将鼠标灵敏度真正接入控制器，支持 ADS 倍率、反转 Y 轴、原始输入设置。

#### P0：枪械

- 先精修 STG44 与 Kar98k：
  - 可控的垂直/水平后坐力曲线；
  - 首发后坐力倍率、连续射击散布、移动/站立/蹲伏修正；
  - 弹速、重力、穿透与距离衰减；
  - 换弹分段事件、拉栓事件、空仓换弹差异；
  - 腰射、ADS、受压状态的准度差异。
- 将射线“瞬时命中”改为步枪子弹可配置：近距离 hitscan，远距离弹道投射体或统一高速弹体。
- 建立部位 hitbox，而不是依赖渲染网格名字或单一包围体。
- 增加命中确认、击杀确认、爆头确认、近失弹 suppression、镜头冲击和音频反馈。

#### P1

- 支持左手/右手视角、FOV 独立设置、武器检视。
- 加入输入缓冲：切枪、换弹取消、开镜、射击之间有明确优先级。

### 验收标准

- 2 把精修武器在 10 m、30 m、60 m 的 TTK、散布、命中率都有自动化靶场数据。
- 60 FPS 与 30 FPS 下射速误差 < 1%。
- 任何视觉动画不能改变服务器认可的射击结果。
- 5 名试玩者能仅凭手感区分两把武器，并能描述其使用场景。

### 阶段退出物

- 靶场地图、移动测试赛道、武器数据表、输入录制回放工具。

### 首批执行记录（2026-08-03）

- [x] 玩家落地判定改为 Rapier raycast，获得地面距离、法线与坡度并限制最大可行走坡度。
- [x] 设置系统支持持久化音量、鼠标灵敏度、ADS 倍率、FOV、反转 Y 轴和画质，PlayerController 已实际消费配置。
- [x] STG44/Kar98k 增加最小伤害、衰减起止、首发后坐力、移动/蹲姿散布；Kar98k 加入拉栓锁定。
- [x] 建立 10m/30m/60m 伤害与 STK、30/60/120 FPS 射速稳定性、姿态散布和拉栓测试。
- [x] 自动化覆盖增至 15 个文件、48 项测试，完整质量门禁通过。
- [x] 站立/蹲姿已同步切换 Rapier Capsule，站起前会检测头顶空间，避免穿模。
- [x] 新增固定步长 `Projectile` 弹道模型，支持初速、重力、寿命、距离与飞行时间计算；实战近距离仍保留射线兼容。
- [x] 新增开火、空仓、换弹开始/完成、拉栓开始/就绪武器动作事件，可直接驱动后续动画与音频。
- [x] 新增可重复 `RangeSimulation`，输出 10m/30m/60m 伤害、STK、理论 TTK 与飞行时间。
- [x] 自动化覆盖增至 18 个文件、56 项测试，完整质量门禁通过。
- [ ] 台阶/攀爬、正式骨骼动画和完整靶场地图仍需继续推进；正式动画和美术品质依赖外部角色/武器资产。

---

## 阶段 3：征服模式、兵种与小队闭环

**目标：** 从“地图里互射”升级为有目标、有职责、有节奏的战局。  
**建议周期：** 2–3 周。

### 任务

#### P0：征服模式

- 明确一局流程：部署 → 倒计时 → 战斗 → 结算 → 重开。
- 完善据点争夺：争夺状态、中立化、占领速度、人数上限、出生保护、前线出生限制。
- 修正阵营归属一致性；当前玩家在征服模式设为盟军，但 TDM 统计中加入 A 队，必须统一。
- 兵力值由服务器/模拟核心统一扣减，死亡、据点优势、载具损失均有事件来源。
- 加入安全出生点选择：敌人距离、视线、爆炸危险、最近占领点。

#### P0：兵种

- 先实现 4 个原型兵种：突击、医护、支援、侦察。
- 每个兵种具有：主武器类别、两件装备、被动能力、补给职责。
- 增加倒地/救援、弹药补给、医疗包、标记/侦察中的核心闭环。
- 取消“出生即拥有全部武器”，改为部署界面选择兵种和有限装备。

#### P1：小队

- 4 人小队、队长命令、队友出生、命令积分、队伍颜色区分。
- 小队补给呼叫或轻量增援，作为高层目标激励。

### 首批执行记录（2026-08-04）

- [x] 新增确定性 `RoundFlow`：部署、倒计时、战斗、结算和自动重开。
- [x] 修正玩家在 TDM 统计中错误加入德军 A 队的问题，统一为苏军/盟军。
- [x] 票数扣减统一为 `death/objective/vehicle` 来源，并发出类型化 `TicketEvent`。
- [x] 新增安全出生评分，综合阵营、敌人距离、视线与爆炸危险。
- [x] 据点状态增加争夺标记和双方人数；争夺时冻结进度，人数优势最多 3 人加速。
- [x] 自动化覆盖增至 20 个文件、63 项测试，完整质量门禁通过。
- [x] 定义突击、医护、支援、侦察四兵种，每类限制一个主武器类别、两件装备、被动与职责。
- [x] 新增倒地窗口、救援、治疗、弹药补给和侦察标记模拟核心。
- [x] 新增最多 4 人小队、自动队长、队长命令、命令积分和脱战队友出生资格。
- [x] 自动化覆盖增至 23 个文件、71 项测试，完整质量门禁通过。
- [x] 新增完整部署 UI：四兵种卡片、职责/主武器/装备详情和部署操作。
- [x] 部署选择已接入 GameScene，出生后仅能使用兵种主武器，不再任意切换全部武器。
- [x] RoundFlow 已接入实际主循环：部署后 5 秒倒计时、战斗、结算、12 秒后重置并返回部署。
- [x] 自动化覆盖增至 29 个文件、83 项测试，完整质量门禁通过。
- [ ] 5 人试玩、12–20 分钟长局、兵种选取率和出生后 3 秒死亡率需要真实玩家与长时间浏览器测试。

### 验收标准

- 一局 12–20 分钟能够稳定结束、结算并重新开始。
- 4 个兵种都有不可替代的团队价值，单一兵种选取率不长期超过 45%。
- 出生后 3 秒内被击杀的比例在测试局中低于 5%。
- 据点与兵力值状态在 UI、模拟和日志中完全一致。

---

## 阶段 4：AI 导航、小队战术与压测替身

**目标：** AI 不再只是追逐玩家，而是能够填充战局、争夺目标并作为联网压测实体。  
**建议周期：** 2–3 周。

### 任务

#### P0

- 基于地图导航网格或 waypoint graph 实现寻路，支持门洞、道路、掩体和不可达检测。
- 把 Bot 改为感知系统 + 决策层 + 动作层：视觉、听觉、最近威胁、目标价值、行动冷却。
- 小队 AI 角色：队长、突击、支援、医护；至少支持跟随、集火、压制、推进、撤退、救援。
- 据点目标选择使用效用评分，考虑距离、友军数量、敌军压力和兵力值。
- 增加 AI LOD：远距离降低感知和决策频率，离屏角色简化动画与碰撞查询。
- 修复 AI 计分板当前随机 Ping 和静态击杀数，所有统计来自战斗事件。

#### P1

- 坦克/车辆 AI、反载具决策、烟雾掩护。
- 难度不通过简单提高命中率作弊，而通过反应时间、误差、沟通和战术水平区分。

### 首批执行记录（2026-08-04）

- [x] 新增 waypoint `NavigationGraph` 与 A*，支持最近节点、最短路径和不可达检测。
- [x] 新增视觉/听觉感知记忆、威胁评分与据点效用选择，考虑距离、归属、压力和票数差。
- [x] 新增近/中/远 AI LOD 预算，远距离离屏 Bot 降低决策频率并关闭动画/碰撞查询。
- [x] 新增事件驱动 `AIStats`，替代随机 Ping 和静态计分的数据基础。
- [x] 自动化覆盖增至 26 个文件、78 项测试，完整质量门禁通过。
- [x] `AIPerception` 与 `AILod` 已接入 AIBot 主循环，按距离与可见性降低感知/决策频率。
- [x] 四人 AI 小队已分配队长、突击、支援、医护角色，每秒生成跟随、集火、压制、推进、撤退、救援决策，并保留可解释日志。
- [x] 新增墙体射线与烟雾体视线约束；失去视线时立即停止追击/攻击，不再隔墙或隔烟持续精准跟踪。
- [x] AI 计分板移除随机 Ping 和静态死亡，改用 `AIStats` 固定延迟与战斗事件统计。
- [x] 新增确定性 16v16、30 分钟长局模拟，调度工作量较全频基线降低超过 50%，模拟卡死率低于 1%，回放结果一致。
- [x] 自动化覆盖增至 31 个文件、89 项测试，完整质量门禁通过。
- [ ] 真实浏览器 16v16 场景的 AI CPU 平均/P95、真实导航卡死率和补给/救援动画仍需实机长测；坦克 AI 属于后续载具阶段。

### 验收标准

- 16v16 Bot 战局连续运行 30 分钟，无长期卡死；不可达卡死率 < 1%。
- Bot 能自主占点、补给、救援、使用掩体，行为日志可解释。
- 中端目标机上 32 Bot 的 AI CPU 平均预算 ≤ 4 ms/帧，P95 ≤ 8 ms。
- AI 不透视烟雾和实体墙，不在无视线时持续精准追踪玩家。

---

## 阶段 5：地图、角色与资源管线升级

**目标：** 把程序化灰盒升级为可优化、可迭代的二战废墟垂直切片地图。  
**建议周期：** 3–5 周，可与美术并行。

### 任务

#### P0：资源管线

- 支持 GLTF/GLB、Draco/Meshopt、KTX2/Basis 纹理、骨骼动画和资源清单。
- 所有资源具备：稳定 ID、版本、LOD、碰撞代理、许可证记录、体积预算。
- 分阶段加载：菜单必需 → 玩家/武器 → 近区地图 → 远区细节/音频。
- 引入缓存和失败占位物；移除仅加载纹理的半成品 `AssetPipeline` 设计。

#### P0：地图

- 重做一张原创“欧洲城市废墟”征服地图，3–5 个据点。
- 先做动线灰盒：步兵侧路、载具主路、反载具高点、室内近战区、狙击视线控制。
- 地图结构分区流送：近区完整加载、远区 HLOD、不可见区剔除。
- 静态碰撞、导航、声音区域和渲染数据来自同一地图源，避免视觉与物理脱节。
- 使用 InstancedMesh 合并瓦砾、植被、路障等重复物；禁止当前“遍历每个 Mesh 自动生成 100 个实例”的做法。

#### P0：角色与动画

- 两阵营至少各 1 套原创士兵模型和装备变化。
- 动画状态：待机、8 向移动、冲刺、蹲伏、跳跃、落地、换弹、开火、受击、倒地、救援。
- 第一人称手臂和第三人称角色分离；武器事件通过动画标记驱动。

#### P1

- 材质湿润度、积雪、弹孔贴花、战壕泥地、风吹布料的简化表现。
- 地图光照烘焙或轻量探针系统，减少动态灯数量。

### 首批执行记录（2026-08-04）

- [x] 新增资源清单：稳定 ID、版本、类型、四阶段加载、LOD、碰撞代理、许可证和体积预算。
- [x] `AssetLoader` 支持 GLTF/GLB 与 Meshopt，提供 Draco/KTX2 配置入口、缓存、并发去重、统计和失败模型占位。
- [x] `AssetPipeline` 改为按清单加载模型/纹理，不再把模型 URL 当纹理预载。
- [x] 新增 `MapDefinition` 统一生成渲染、碰撞、导航和声音区数据；重复物只按显式实例组创建。
- [x] 删除 `PerformanceOptimizer` 遍历每个 Mesh 生成 100 个实例和重复克隆 LOD 的危险逻辑。
- [x] 新增角色动画状态机和动画标记，覆盖待机、移动、冲刺、蹲伏、跳跃、落地、换弹、开火、受击、倒地、救援。
- [x] 自动化覆盖增至 35 个文件、96 项测试，完整质量门禁通过。
- [ ] 正式原创欧洲城市地图、两阵营士兵/第一人称手臂、武器 GLB、KTX2 纹理和动画片段需要美术资源后才能完成实装。
- [ ] 统一 MapDefinition 已可用，但现有 BerlinRuins 仍需渐进迁移并补充分区流送/HLOD。

### 验收标准

- 目标地图可完整游玩，所有关键通道都有碰撞与导航验证。
- 近景主武器、角色、载具无明显材质缺失或动画穿模。
- 中画质基准场景 draw calls P95 ≤ 350，动态三角形按场景预算控制；不再沿用当前过低且未经场景验证的 100 draw calls/10 万三角形硬编码预算。
- 首次战局加载和峰值内存符合第 3.3 节目标。

---

## 阶段 6：战场渲染、特效与音频

**目标：** 用可控成本建立“战争大片感”，而不是只增加粒子数量。  
**建议周期：** 2–4 周。

### 任务

#### P0：渲染

- 使用正确的色彩管理、曝光、色调映射和雾效；定义晴天/阴天/黄昏三套美术基线。
- 阴影采用分级策略：玩家和近处载具高质量，远处关闭或使用 blob/contact shadow。
- 增加动态分辨率和分档像素比，根据 P95 帧时间平滑调整。
- 天气系统与材质联动：雨天湿润、雾天能见度、雪天粒子与地面色调变化。

#### P0：特效

- 建立统一 VFX 池：枪口火焰、曳光弹、火花、尘土、爆炸、烟雾、火焰、弹孔。
- 特效按距离、屏幕占比、重要性分级；离屏立即休眠或回收。
- 烟雾既影响视觉，也进入 AI 感知和玩家标记逻辑。
- 爆炸具备冲击波、相机震动、耳鸣、扬尘和物理冲量，但分别设预算。

#### P0：音频

- 用合法录制/购买素材替换程序合成占位音。
- 枪声分层：机械声、枪口爆发、远场尾音、室内反射、弹壳、呼啸/近失弹。
- 依据空间区域切换室内/室外混响和低通；爆炸按距离产生延迟与频谱变化。
- 添加战场 ambient、远处炮火、载具引擎、语音提示、材质脚步。
- 增加并发数、优先级和虚拟化，防止 32 人枪声压垮音频节点。

### 验收标准

- 玩家闭眼能区分武器类别、枪声大致方向和距离层级。
- 32 个并发战斗实体时无持续音频爆音、明显断裂或节点泄漏。
- 中画质下大规模爆炸不会使 P95 帧时间连续 2 秒超过 25 ms。
- 雾和烟不会只对玩家生效而对 AI 无效。

### 首批执行记录（2026-08-05）

- [x] 新增 `VisualProfile` 三套美术基线（晴天/阴天/黄昏）：曝光、天空/雾色、FogExp2 密度、环境光与方向光强度、太阳色温、阴影强度、太阳方向及天气兼容矩阵；新增色温→RGB（Tanner Helland）近似与按天气/时刻解析基线函数。
- [x] 新增 `WeatherMaterialLink` 天气-材质联动：雨天降低粗糙度模拟湿润，雪/雾/沙尘做色调混合与亮度缩放；材质原值只记录一次，`restore()` 可整体还原。
- [x] 新增 `DynamicResolution` 动态分辨率：按帧时间带死区、滞回与冷却逐档调整渲染比例，避免画质在临界帧率抖动，为后续接入 `renderer.setPixelRatio` 提供接口。
- [x] 新增统一 `VfxPool` 特效池：枪口火焰、曳光弹、火花、尘土、爆炸、烟雾、火焰、弹孔共用池化句柄；全局与单类型预算、重要性淘汰、near/mid/far/hidden 距离分级、离屏超时自动回收，替换散落 `new Points` 的基础已具备。
- [x] 新增 `ExplosionImpact` 爆炸冲击：物理冲量、相机震动、耳鸣、扬尘四通道独立每秒预算，超预算自动降级并记录 `degraded` 通道（调试可解释）。
- [x] 新增 `GunshotLayers` 枪声分层：机械声/枪口爆发/远场尾音/弹壳四层，各自可听距离与平方衰减，远处只保留尾音，避免 32 人枪声互相淹没。
- [x] 新增 `AudioVoiceManager` 音频并发管理：真实/虚拟 voice 上限、优先级抢占、超出可听距离自动虚拟化、距离变化实时真实↔虚拟转换，防止音频节点数失控。
- [x] 自动化覆盖增至 42 个文件、143 项测试，完整质量门禁 `npm run check` 通过。
- [x] **第二批接线（已接入主循环）**：渲染器启用 ACES 胶片色调映射 + sRGB 输出 + 曝光基线；`VisualProfile` 按天气与时刻每帧切换曝光/太阳色温/环境色/阴影强度（昼夜开启时太阳角度仍由 WeatherSystem 驱动）；`WeatherSystem` 新增 `onWeatherChange` 回调，天气切换时 `WeatherMaterialLink` 收集场景材质应用湿润/色调/亮度覆盖并可整体还原；`VfxPool` 作为爆炸/烟雾特效的预算门控（EXPLOSION/SMOKE 单类型预算，超限跳过表现）；`ExplosionImpact` 四通道接线——相机震动（预算限幅）、耳鸣（新增 TINNITUS 合成音，音量随强度）、物理冲击（对近距离 Bot 施加击退/受击反馈，不扣血）、扬尘（新增灰尘粒子）；`GunshotLayers` 枪声分层 + `AudioVoiceManager` 预算接入——玩家与 AI 枪声按距离解析可听层、逐层音量、voice 满员自动虚拟化；`AudioSystem.play` 支持音量覆盖参数；`DynamicResolution` 按帧时间动态调整像素比（低画质固定 1x，中/高生效，画质切换时重置）。
- [x] **第三批收尾（已接入主循环）**：载具武器爆炸闭环——吉普机枪（直射/快速）与坦克主炮（弹道/爆炸）发射炮弹实体（重力+寿命+环境/实体命中检测），命中爆炸走 VfxPool 门控 + ExplosionImpact 四通道 + AoE 伤害（Bot/玩家/其他载具）+ 烟雾 + 破坏物联动，坦克爆炸命中载具自动登记重生；玩家射击命中载具（模块化伤害）与可破坏物（阶段 7）均已在 `processHit` 分支处理；性能面板 F3 新增 VOICE real/virtual 与 VFX active 预算统计。
- [ ] VFX 句柄与视觉对象生命周期仍解耦（预算门控已生效，句柄淘汰暂不同步移除视觉对象），留待渲染表现专项。
- [ ] 合法录制音频素材替换合成音、室内/室外混响与低通、战场 ambient/远处炮火/载具引擎仍需外部素材与音频区域数据。

---

## 阶段 7：载具、反载具与局部破坏

**目标：** 建立步兵—载具—反载具克制链，补齐《战地》式沙盒战场的关键特征。  
**建议周期：** 3–4 周。

### 任务

#### P0：载具

- 优先实现 1 辆轻型坦克 + 1 辆运输车，暂停继续扩充载具数量。
- 坦克采用轮/履带接地模型或稳定的定制运动学控制，不能继续只对方盒刚体施加巨大 impulse。
- 载具模块化伤害：车体、履带/轮胎、炮塔、引擎；不同角度与武器有装甲修正。
- 炮塔旋转、炮弹弹道、爆炸半径、同轴机枪、弹药与维修形成完整闭环。
- 进入/离开座位、座位切换、被摧毁逃生、载具重生均由状态机控制。

#### P0：局部破坏

- 采用预切片/状态切换，不做任意体素破坏。
- 可破坏对象首批：木门、沙袋、木栅栏、小型掩体、建筑外墙预设块。
- 破坏结果影响碰撞、导航、视线和网络状态；碎片仅客户端表现，不进入长期同步。
- 破坏状态使用稳定对象 ID + bitset/事件日志同步。

#### P1

- 维修工具、反坦克火箭、地雷、载具补给站。
- 火焰蔓延和更多建筑破坏状态。

### 验收标准

- 坦克击中正面、侧面、后部产生可解释且一致的伤害差异。
- 破坏前后渲染、物理、AI 导航和网络一致。
- 载具碰撞在 30/60 FPS 下稳定，不持续翻车或穿地。
- 16v16 战局中载具不长期垄断，步兵存在明确反制手段。

### 首批执行记录（2026-08-05）

- [x] **载具运动学地面控制**：替换“只对方盒刚体施加巨大 impulse”——每帧地面射线贴地（防穿地/防弹跳）、车身过度倾斜自动回正（只保留 yaw 并清零俯仰/横滚角速度，防翻车）、横向速度强阻尼钳制、前向驱动力按帧时间缩放；吉普高速、坦克低速高惯量，控制手感分层。
- [x] **模块化伤害 + 装甲修正**：车体/履带/炮塔/引擎四部位独立血量（部位血量 = 整车血量×系数，部件可在整车被摧毁前先失效）；命中点相对车体方位计算受击面——正面 0.8 / 侧面 1.25 / 后部 1.6 修正 × 基础装甲；引擎损坏减速 60%、履带损坏转向失灵 75%、炮塔损坏禁用武器；`takeDamage` 返回部位/方向/是否击杀供 HUD 反馈。
- [x] **炮塔旋转 + 炮弹弹道 + 弹药/维修闭环**：炮塔组（炮塔+炮管）平滑跟随准星方向；炮弹实体带重力/寿命/环境与实体命中检测；主炮 1.5s 装填、30 发、爆炸半径 7m AoE；吉普机枪 120ms 射速直射；5 秒未受击自动缓慢维修 + 弹药缓慢补充。
- [x] **座位状态机**：司机/乘客槽位系统、E 上下车、F 键座位切换（乘客与司机互换语义）、被摧毁强制逃生（掉血+震屏）、摧毁后 15 秒重生队列自动复位血量/部位/弹药/位置。
- [x] **局部破坏 P0（`DestructibleSystem`）**：预切片/状态切换（不做任意体素破坏）——木门/沙袋/木栅栏/小型掩体四类；完整对象参与碰撞与 AI 视线，摧毁后隐藏并移出 `environmentObjects`（玩家弹道与 AI 视线同时失效）；碎片仅客户端表现（飞散动画、自动回收）；破坏状态用稳定 ID + bitset（`getStateBitset`/`applyStateBitset`）表达，为阶段 8 网络同步预留；玩家射击与坦克爆炸均可破坏。
- [x] **反坦克火箭（步兵反载具链）**：新增 `PANZERFAUST` 装备——高速直射弹道实体（复用炮弹管线，kind='rocket'），命中环境/实体即爆；对载具 3.5 倍 AoE 伤害、对 Bot/玩家常规 AoE；`applyExplosion` 泛化为（位置/半径/伤害/载具倍率/排除发射者）供主炮与火箭共用；Q 键循环新增该装备。
- [x] **AI 反载具闭环（第二批）**：`AIVehicleTargeting` 纯函数决策（最近敌方载具 + 威胁切换优先级：无目标直打、载具进入 18m 威胁距离或显著更近才放下当前目标）+ `AISystem` 400ms 周期扫描指派目标；载具新增 `team` 阵营（生成时分配，友方不被打、敌方被集火）；assault 兵种携带火箭筒（10s 冷却、对载具 3.5 倍率弹道爆炸）；普通枪弹命中载具走模块化伤害（装甲修正生效）；友军 AI 不打玩家但会反制敌方载具。自动覆盖：`AIVehicleTargeting.test` 10 项。
- [x] **载具弹药维修站（P1）**：`VehicleSystem.addSupplyStation` 按阵营部署在双方营地（视觉：平台 + 阵营色立柱）；同阵营载具进入半径快速维修（80 HP/s）+ 补弹（停车 30 发/s）；进入/离开 HUD 提示。
- [x] **反坦克地雷（P1）**：`MineSystem` 独立系统 + Q 循环新增 MINE 装备（每队上限 3 颗）；敌方载具进入 1.8m 触发半径即爆炸，复用 `applyExplosion`（AoE + 四通道冲击 + 特效 + 破坏物联动，载具 3.2 倍率）；自动覆盖 `MineSystem.test` 8 项。
- [x] 自动化覆盖增至 46 个文件、177 项测试（新增 `AIVehicleTargeting.test` 10 项、`MineSystem.test` 8 项、`VehicleSystem.test` 补给站/阵营 4 项），完整质量门禁 `npm run check` 通过。
- [ ] AI 不驾驶/不乘坐载具（AI 反载具已闭环，驾驶留待后续）；坦克接地为“贴地+回正”简化模型，未实现履带悬挂/地形跟随精度打磨。
- [ ] 破坏状态尚未接入网络快照（阶段 8 落地）；地图破坏物为固定点位散布，尚未与建筑门洞/外墙预设块联动。

---

## 阶段 8：权威多人服务器与同步

**目标：** 从位置广播 Demo 升级为可验证的一致性对战。  
**建议周期：** 4–6 周。

### 任务

#### P0：服务器架构

- 将服务器独立为 Node 服务项目/工作区，共享纯 TypeScript 协议和模拟数据定义。
- 服务端权威管理：房间、玩家、队伍、出生、移动约束、血量、射击、装备、载具、据点、兵力值、回合。
- 二进制协议或紧凑序列化，消息带 `protocolVersion`、tick、sequence、ack。
- 固定服务器 tick：先 30 Hz，快照 15–20 Hz；根据压测再调整。
- 房间生命周期：创建、加入、满员、加载、开始、中途加入、断线保留、结束、销毁。

#### P0：客户端网络体验

- 本地玩家：客户端预测 + 输入序列 + 服务器校正 + 平滑误差回收。
- 远端实体：快照缓冲 + 100–150 ms 插值；丢包时短时外推，超时冻结。
- 命中判定：服务端验证射速、弹药、姿态、可见性；对步兵射击加入有限历史回溯。
- Interest Management：按分区、距离和事件重要性发送实体状态。
- 网络模拟面板：延迟、抖动、丢包、乱序、限速。

#### P0：安全

- 输入速率限制、协议 schema 校验、房间权限、异常移动/射速检测。
- 客户端 `AntiCheat` 只能作提示，不能作为信任根；关键结果全部服务器裁决。
- 记录最小化审计日志，避免存储敏感数据。

### 验收标准

- 16v16，模拟 100 ms RTT、30 ms jitter、2% packet loss 时：
  - 无持续瞬移；
  - 本地移动校正 P95 < 0.5 m；
  - 据点、血量、弹药、击杀最终一致；
  - 断线重连可恢复战局。
- 非法射速、无限弹药、超速移动、伪造击杀均被服务器拒绝。
- 单房间服务器 CPU/内存数据可监控，并完成 2 小时 soak test。

### 执行记录（2026-08-05）

- [x] **第一批：服务器架构 + 共享协议**：`shared/` 二进制编解码（协议版本校验、tick/seq、schema 校验拒绝畸形帧）+ 消息 schema（hello/join/input/snapshot/pong/error）；`server/` SimClock 固定 30Hz tick（掉帧追赶 + 上限防螺旋）、PlayerSim 权威移动（速度钳制/边界修正/射速裁决）、RoomManager（房间/队伍平衡/断线保留 30s/重连恢复）、ServerApp（WebSocket 握手→加入→输入速率限制 40/s→快照 15Hz 广播）；`src/network/` NetClient（输入 seq、RTT/抖动统计、快照缓冲插值 100ms）、NetSimulator（延迟/抖动/丢包/乱序/带宽限速）、SnapshotBuffer（双帧插值 + 冻结标记）。
- [x] **第二批：客户端预测/校正 + Interest Management**：`ClientPrediction` 输入缓冲 + 本地预测（移动模型与服务端 PlayerSim 完全一致，俯仰钳制常量共享）+ 快照校正（ackSeq 回滚重放未确认输入，误差 >0.5m 硬校正、小误差平滑收敛，渲染/权威状态分离）；`SnapshotPlayer` 新增可选 `ackSeq`（codec 存在标志编码，兼容旧快照）；`shared/interest` 按观察者裁剪（本人恒可见 + 120m 视野半径 + 每快照 24 实体上限），ServerApp 快照改为每观察者分别裁剪。
- [x] **第三批：服务端权威射击与命中裁决**：`ProjectileSim` 服务端直线弹道（方向由 yaw/pitch 计算，子步进防隧穿，寿命/射程到期消散，一弹一命中），开火（射速裁决通过）生成弹丸 → 每 tick 推进 → 命中回调扣血；伤害/击杀全部服务器裁决，客户端无法伪造击杀。
- [x] **第四批：客户端断线自动重连**：NetClient 传输改工厂模式（重连重建 WebSocket），指数退避重连（base×2^attempts），重连后自动重新握手并恢复原房间（配合 RoomManager resumed 语义）；主动 disconnect 不重连，次数耗尽回调 max_reconnects，握手成功复位计数。
- [x] **第五批：F4 网络模拟调参面板**：`NetSimPanel` 独立于 F3 性能面板，5 个实时滑块（延迟/抖动/丢包/乱序/带宽，带显示换算）+ 实时统计（SENT/RECV/DROP/REORD/DELAY），jsdom 单测。
- [x] **第六批：快照丢包短时外推**：SnapshotBuffer 渲染时间超最新快照且在外推窗口内（默认 200ms）按最后两帧速度外推位置/朝向，新玩家/死亡玩家冻结在最新帧，超窗回退冻结。
- [x] **第七批：端到端回环集成验证（NetPlay）**：`server/NetPlay.test.ts` 本地起权威 ServerApp（随机端口）+ 双 NetClient 经真实 WebSocket（ws 包 NodeTransport）验证全链路互操作：① A 前进 B 快照互见（一致性）② 1 秒内超发 60 输入被拒（input_rate_limited 裁决）③ 断线自动重连 + join_ack.resumed=true 恢复战局 + 重连后继续收快照 ④ NetSimulator 60ms 延迟包装下 RTT ≥ 40ms。配套生产修复：ServerApp.stop 停止 tick 循环（优雅关闭）、NetClient 新增 transportFactory 注入（同构/重连复用）与 dropConnection 调试 API、disconnect 清理 ping 定时器、server/tsconfig 补 DOM lib 以交叉类型检查客户端代码。
- [x] **第八批：16v16 压测 + soak 监控 + 安全验收 + 网络健壮性修复**：`server/HeadlessClient.ts`（虚拟玩家：复用 NetClient + 双向网络模拟，随机输入循环）+ `server/LoadTest.test.ts`（32 客户端满员入房，8 个挂 100ms RTT/30ms jitter/2% loss，验证稳定 tick、持续收快照、模拟组丢包缺口、RTT 反映延迟、内存增长可控）+ `server/Security.test.ts`（9 项：未握手 join/未入房 input/客户端发服务端消息/未知 tag/截断帧/协议不匹配断开/乱序缓冲/旧包静默/seq 跳跃拒绝）+ `server/soak.ts`（CLI：周期打点 tick/RSS/heap/RTT/错误，汇总 P50/P95，生产验收可跑 2 小时）。**顺带修复 6 处真实网络健壮性缺陷**：① RawTransport 缺 connect()（模拟器包装下 hello 在 ws open 前发出被丢）② ping 循环在未连接时终止（双向延迟下永久无 RTT）③ hello 无重试（首包丢则连不上）④ join 无重试 ⑤ 服务器拒绝乱序输入（30ms jitter 制造真实乱序）→ INPUT_BUFFER_WINDOW=8 缓冲按 seq 应用，重放静默丢弃，超窗 input_jump 拒绝 ⑥ 网络模拟只包出站 → 双向对称。
- [x] **第九批：GameScene 网络接入（替换位置广播原型）**：新增 `NetworkGameClient` 桥接层（握手→join→输入序列发送→快照分发，自动排除本地玩家，`transportFactory` 可注入）+ `RemotePlayerView` 远端渲染视图（快照插值姿势集合，离开/快照消失自动清理）；`NetClient` 补齐 `onPlayerLeave` 回调；GameScene 连接改走新协议栈——本地玩家按键/瞄准按服务器 tick 频率（30Hz）发送 `input`（服务端权威移动裁决），远端玩家由快照缓冲 100ms 插值渲染（替换旧版 JSON 位置广播直接瞬移），`player_leave` 即时移除 mesh，dispose 完整清理远端 mesh；配置新增 `network.roomId`（默认 `lobby-1` 对齐权威服务器），`serverUrl` 默认指向 `ws://localhost:8787`；`NetSimulator` 新增可选 `seed`（mulberry32 确定性 PRNG，丢包/抖动/乱序可复现），16v16 压测模拟组固定种子 + 组级丢包缺口断言，消除单客户端 0 缺口的概率性抖动。
- [x] **第十批：服务端征服规则权威化（游戏内容权威化第一步）**：新增 `server/ConquestSim.ts`（服务端权威征服状态机：双方兵力值、击杀计数、3 据点捕获进度与归属翻转、无据点方每秒流失、胜负判定；纯逻辑可单测）；`shared/protocol.ts` 新增 `ServerGameState` 消息（tickets/kills/objectives/winner/phase，协议约定 team 0=德军红、team 1=苏军蓝），`codec.ts` 注册 tag=9 紧凑二进制编解码（u16 兵力/击杀、int16 捕获进度）；ServerApp 接线——首个玩家加入即开局（room.start + 创建 ConquestSim）、弹道命中死亡翻转时 `onPlayerKilled` 扣兵力记击杀、每 tick 规则推进、`game_state` 按 2Hz（tick%15）广播；`NetClient`/`NetworkGameClient` 透传 `onGameState`；GameScene 联网模式下 HUD 兵力条与据点显示改用服务端权威数据（本地 AI 对局模拟不再影响显示，场景据点视觉后续切片统一）。**端到端验证 `server/GameState.test.ts`**：加入即开局广播 + 队 0 瞄准队 1 连射 → 服务端裁决死亡 → 扣兵力 + 击杀计数 → game_state 反映（真实 WebSocket，pitch 下压公式 `asin(Δy/dist)` 命中躯干）。
- [x] **第十一批：本地预测驱动接入渲染（联网手感闭环）**：`NetworkGameClient` 新增 `prediction` 选项透传 `NetClient`（sendInput 自动推进本地预测、快照按 ackSeq 自动校正——移动模型与服务端 PlayerSim 完全一致）；`PlayerController` 新增 `teleportHorizontal(x,z)` 权威回写接口（保留 y 与刚体速度，不破坏跳跃/下蹲/碰撞）；`GameScene` 联网模式下每帧把本地玩家渲染位置向预测轨迹平滑收敛（`NETWORK_POSITION_CONVERGE=0.2`，服务端无碰撞模型的水平偏差由快照校正吸收；垂直由本地物理负责），单机模式零影响。**验证**：新增 2 项 NetworkGameClient 透传测试（sendInput 推进预测位移 ≈ 5.2×5/30m；快照触发 onPredictionReconcile 校正）。已记录已知项：服务端快照未携带 ackSeq，预测输入缓冲不会按 ack 清理（后续协议切片补，不阻塞渲染闭环）。
- [x] **第十二批：服务端 ackSeq 语义修正 + 端到端验证（预测缓冲正确清理）**：核查发现 ackSeq 链路（协议字段 → codec 编解码 → ClientPrediction.ack → reconcile）早已就绪且有单测，但服务端广播快照时用 `conn.lastSeq`（收到最高 seq）而非 `conn.lastAppliedSeq`（已应用最高 seq）——乱序缓冲中尚未应用的输入会被错误确认，客户端丢弃后硬校正重放会状态偏差。修正为 `lastAppliedSeq` 并在 `NetPlay.test.ts` 新增端到端用例：客户端挂 ClientPrediction 发送输入 → 服务端应用 → 快照携带 ackSeq → reconcile 按 ack 清空预测缓冲 → 位移与同模型预测一致（真实 WebSocket 闭环）。
- [x] **第十三批：服务端据点视觉与客户端场景统一（布局同源）**：发现第十批遗留错位——服务端权威据点是 `alpha/bravo/charlie`（15,15 / 0,0 / -15,-15），客户端场景据点是 `A/B/C`（0,-30 / 0,0 / 0,30），HUD 已切服务端数据但场景旗点位置/标签对不上。本轮：① 权威布局 `CONQUEST_OBJECTIVE_DEFS` 进 shared（协议层），服务端 ConquestSim 改为复用同源常量（消除漂移源）；② 场景据点视觉重构为按 id 索引的三件套（圆圈/旗杆/标签），联网模式（收到 game_state）自动重定位到权威布局并重绘标签，归属颜色由 `objectives` 驱动（旗杆也随归属上色）；③ 抽 `objectiveOwnerToTeam` 纯函数消除 HUD/场景重复转换。单机 AI 对局保持本地 A/B/C 布局不受影响。测试：`objectiveOwnerToTeam` 3 值用例 + game_state 据点 id 与服务端权威定义同源断言（端到端防漂移）。
- [x] **第十四批：服务端载具权威化（数据面 + 广播 + 端到端）**：联网模式此前完全没有载具概念（纯单机玩法）。本轮：① shared 新增权威载具重生点 `VEHICLE_SPAWN_DEFS`（与据点布局错开、贴近双方出生点）+ 4 条协议消息（`vehicle_state`/`vehicle_enter`/`vehicle_exit`/`vehicle_drive`）与 codec 编解码；② 新增 `server/VehicleSim.ts` 纯逻辑运动学模拟（与 PlayerSim 同坐标系：加速度钳制/转向/解析积分、司机位占用、受击摧毁 → 15s 重生复位），可单测；③ ServerApp 集成：join 初始化、上车距离校验（8m）、驾驶输入每 tick 应用到司机所在载具、随快照周期（15Hz）广播 vehicle_state、断线自动下车；④ NetClient 支持载具消息；⑤ 测试：VehicleSim 单测 6 项、codec 往返 2 项、端到端 3 项（初始广播/上车→驾驶→位置推进→下车/距离过远拒绝——修了两处测试假设：输入限速 40/s 用 30Hz、队伍平衡分配导致出生点不确定按 team 选目标载具）。客户端联网视觉/交互接入为第十五批。
- [x] **第十五批：客户端联网载具视觉与交互（服务端权威驱动闭环）**：上一批服务端数据面就绪后，本轮接入客户端渲染与交互。① 新增 `src/vehicle/NetworkVehicles.ts` 纯视觉管理器：按 vehicle_state（15Hz）创建/更新/移除载具 mesh（车身按配置尺寸 + 四轮 + 顶置武器：坦克主炮塔/其他机枪座），归属色（红/蓝/灰）与摧毁变暗，平滑插值（15Hz → 60fps 渲染），交互查询（findNear 只返回未摧毁、getByDriver 匹配司机）；② NetworkGameClient 透传 onVehicleState + sendVehicleEnter/Exit/Drive；③ GameScene 联网分支：connectToServer 创建视觉管理器并移除本地单机载具（权威化不混用）、按 E 上车（发 vehicle_enter，交互半径 4m < 服务端校验 8m）、驾驶状态由服务端 driverId 驱动（进/出驾驶、被挤下车/载具摧毁被动退出）、第三人称相机跟随、驾驶输入路由 sendVehicleDrive、下车就近落地；④ 单机模式零影响（全部走原 VehicleSystem 分支）。测试：NetworkVehicles 单测 5 项（创建/插值收敛/差集清理/交互查询/变色）+ NetworkGameClient 透传 2 项。修复两处：createVisual 首次上色未按归属（updateTarget 变色条件首次不触发）、测试缺 hello_ack 注入导致 connected 守卫丢弃发送。载具武器/伤害联网闭环为后续批次。
- [x] **第十六批：载具武器/伤害联网闭环（服务端权威弹道裁决）**：补齐载具开火全链路。① 协议：`vehicle_fire`（C→S：vehicleId + aimYaw/aimPitch + weaponIndex）；`VEHICLE_SIM_CONFIGS` 上移 shared 同源（label/maxSpeed/health/hitRadius/weapons[]：吉普机枪 14 伤 140ms 冷却、坦克主炮 120 伤 1800ms 冷却、卡车/摩托无武器）；② 服务端：VehicleSim.fire 裁决司机/武器/冷却（每武器 lastFireAt）→ 返回发射参数；ServerApp.handleVehicleFire 生成弹丸（ownerId=司机，复用玩家弹道管线）；载具作为 ProjectileSim 目标（radius 按车型 1.6/2.5）——玩家与载具弹均可命中载具（载具正确挡弹），命中走 takeDamage → 摧毁清空司机 + 重生计时（vehicle_state 广播驱动客户端被动下车）；③ 客户端：驾驶开火按武器冷却节流发送（方向=载具朝向）、NetworkVehicles 本地炮口闪光 + 枪声即时反馈、HUD 载具血量条（setVehicleHealth/hideVehicleHealth）；④ 测试 +6：VehicleSim.fire 裁决（司机/冷却/摧毁/非法索引）、ProjectileSim radius 大型目标命中、codec vehicle_fire 往返、NetPlay 端到端 2 项（吉普机枪命中玩家掉血、坦克双炮摧毁吉普清司机）。排查中确认两处设计行为：v1/v2 对角线共线时弹道被载具正确拦截（GameState 击杀用例 target 走位避开）；坦克第二炮需等 1800ms 冷却。门禁 68 文件 / 350 项全绿。
- [x] **第十七批：玩家死亡/重生/击杀反馈与回合重启（服务端权威对战闭环）**：联网模式此前死亡即永久躺尸、无击杀反馈、回合结束不重开，直接卡住"可验证的一致性对战"验收。① 协议：`kill_feed`（tag=18：击杀者/受害者/武器名，环境击杀 killerId=null）+ `RESPAWN_DELAY_MS=3000`/`ROUND_RESTART_DELAY_MS=5000` 常量；② 服务端：弹丸携带武器显示名（步枪/机枪/主炮，VehicleSim.fire 结果补 weaponKind 映射）→ 命中死亡翻转广播 kill_feed（双方即时可见）并扣兵力记击杀；PlayerSim 记录 deathTimeMs，ServerApp 每 tick 检查重生计时 → 队伍出生点复活（仅 started 阶段：结算期保持阵亡，由 restartRound 统一复活）；回合结束（winner）→ room.end + roundEndAtMs → 结算期后自动开新回合（重置 ConquestSim/VehicleSim、全员复活、立即广播 room_state/game_state）；③ 客户端：NetClient/NetworkGameClient 透传 kill_feed + 新增 getOwnState（快照校正后的本人 health/alive/位置，服务端权威）；GameScene 联网分支——死亡翻转复用单机死亡遮罩/音效 + 重生倒计时、服务端复活时传送本地玩家到权威出生点、输入发送按网络存活门控、HUD 血量改用服务端裁决值、K/D 由 kill_feed 统计（击杀列表复用 addKillMessage）、新增回合结算遮罩（胜者 + 新回合倒计时，game_state 驱动显示/隐藏）；④ ClientPrediction 修复：reconcile 小误差路径只回写渲染状态，health/alive 不参与预测必须始终回写权威值（死亡检测依赖 prediction.state）。测试 +5：codec kill_feed 往返（含空击杀者）、PlayerSim 死亡时刻/重生复位、NetworkGameClient kill_feed 透传 + getOwnState 快照回写、新 `server/CombatLoop.test.ts` 端到端 2 项（真实 WebSocket：瞄准连射 → 死亡 → 双方收 kill_feed → 3s 后队伍出生点复活；兵力打空 → winner+ended 广播 → 结算期 → 新回合 phase=started/兵力重置/全员复活）。**顺带修复压测夹具缺陷**：HeadlessClient.start() 用 stats.roomId 判断入房完成，但该字段在 joinRoom() 调用时即赋值——join 丢包重试期间输入抢跑触发 not_joined（16v16 全量并行偶发），改为等待 join_ack（joined 标志）。门禁 69 文件 / 355 项全绿。
- [x] **第十八批：破坏物状态网络同步（服务端权威 + 弹道挡弹）**：联网模式破坏物此前纯客户端本地裁决（各端状态分歧、可穿沙袋击杀），本轮落地计划标注"阶段 8 接入快照"的破坏同步。① 布局同源：shared 新增 `DESTRUCTIBLE_KIND_CONFIGS`（四类血量/尺寸）+ `DESTRUCTIBLE_SPAWN_DEFS`（8 个点位，与 GameScene 原硬编码完全一致）——服务端裁决与客户端渲染/碰撞共用，消除漂移；② 服务端：新 `server/DestructibleSim.ts`（纯逻辑权威破坏：受击扣血/摧毁、bitset 表达、reset）；弹道推进（ProjectileSim.step 扩展 obstacles 参数 + onObstacleHit 回调，签名向后兼容）——弹丸与破坏物做**旋转矩形线段相交**（slab 法：线段变换到对象局部坐标系求交，支持 rotationY；垂直范围过滤 centerY±halfHeight），命中优先于目标裁决：弹丸消散、破坏物扣血（步枪伤害 25/发）、已破坏对象不挡弹；ServerApp 每 tick 组装全房间障碍列表（附 roomId 定位）、状态变化时广播 `destructible_state`（tag=19，bitset 字符串，变化去重）、restartRound 重置并广播全 0；③ 客户端：DestructibleSystem 配置改从 shared 引用（DESTRUCTIBLE_CONFIGS = shared 同源）+ 新增 reset()（恢复完整并清碎片）；NetClient/NetworkGameClient 透传 onDestructibleState；GameScene 联网模式**禁用本地破坏裁决**（射击/爆炸 AoE 命中破坏物一律等服务端 bitset 驱动，避免分歧）、onDestructibleState → applyStateBitset（只破坏不回滚）、room_state phase ended→started 检测新回合 → reset() + 重建 environmentObjects（破坏物重新参与碰撞与 AI 视线）；④ 测试 +7：DestructibleSim 6 项（布局一致性/受击扣血/bitset/已破坏不重复受击/非法 id/重置/障碍尺寸）、ProjectileSim 挡弹 4 项（挡弹优先不伤目标、已破坏不挡、垂直范围过滤、旋转矩形局部轴判定）、codec destructible_state 往返、NetworkGameClient 透传、CombatLoop 端到端（真实 WebSocket：射手朝沙袋连射 → 双方收 destructible_state bits[0]='1'）。**顺带稳化既有 flaky 测试**：NetPlay 载具机枪用例原 ±0.05 扇形扫射，全量并行高负载下 B 移动窗口位移不足导致漏判，加宽为 [1.85,2.40] 覆盖 B 移动 0%–100%（stash 验证与本次改动无关）。门禁 70 文件 / 369 项全绿。
- [x] **第十九批：有限历史回溯（服务端命中按发射时刻裁决）**：联网模式此前命中裁决用弹丸到达时刻的目标位置，而射手瞄准的是客户端插值缓冲显示的开火时刻位置——100ms RTT 下移动目标必然打空，双方击杀认知不一致。本轮落地计划标注的 lag compensation。① 共享常量 `HIT_REWIND_WINDOW_MS=500`（回溯窗口，超窗采样返回 null，调用方回退当前帧）；② 新 `server/PositionHistory.ts`（按 tick 记录玩家位置 + 二分线性插值采样 + 容量裁剪 + 乱序/同 tick 防御，纯逻辑可单测）；③ `ProjectileSim`：弹丸记录 `spawnTimeMs`（发射时刻），`ProjectileTarget` 新增可选 `sampleAt` 回调——命中判定用开火时刻的目标回溯位置裁决，载具/无历史目标回退当前帧（向后兼容）；**回溯只改目标位置不改弹道路径**——破坏物挡弹检查照常生效，穿掩体击杀不可能；④ ServerApp 每 tick（输入应用/无输入/重生）记录玩家位置，玩家目标挂 sampleAt，玩家与载具开火均传 spawnTimeMs；⑤ 测试 +12：PositionHistory 8 项、ProjectileSim 回溯 3 项（回溯命中/无历史回退/回溯位置带外不命中）、CombatLoop 端到端 1 项（真实 WebSocket：target 走位避开载具对角线后静止，shooter 单发瞄准当前位置，target 同时移动——弹丸飞行 0.67s 内移动 3.5m >> 0.6m 命中半径，回溯命中 → health 75，非回溯必 miss）。**顺带稳化两处高负载 flaky**（soak 并行 + 全量并行下偶发，stash 验证与改动无关）：NetPlay 载具摧毁用例改事件驱动（waitFor 第 1 炮伤害后等满主炮冷却再发第 2 炮）、ackSeq 用例断言改轮询宽限。门禁 71 文件 / 381 项全绿。
- [ ] **命中可见性（地图静态遮挡）**：不可破坏建筑/墙体目前仅客户端随机生成（BerlinRuins `gameplayRandom()`），服务端无确定性几何可裁决——需先地图确定性化（静态遮挡布局进 shared 同源），列为阶段 9 地图专项；**16v16 100ms RTT 完整验收**（soak 脚本已就绪，2 小时运行中）。
- [x] 自动化覆盖 71 文件、381 项测试全绿（`npm run check` 门禁：format + lint + 双 typecheck + 全量测试 + build:check）。

---

## 阶段 9：性能专项与大规模战场压测

**目标：** 把性能从“有优化类”变为“有预算、有数据、有自动降级”。  
**建议周期：** 2–3 周。

### 任务

#### P0

- 使用 Chrome Performance、Three.js renderer info、内存快照建立 CPU/GPU/GC 分析流程。
- 拆解帧预算：模拟、AI、物理、动画、VFX、UI、提交 draw call、GPU。
- 避免每帧分配：Vector、数组、Canvas 更新、临时对象、字符串和事件对象进入池化或复用。
- 小地图、计分板、AI 血条、据点 UI 降频；只在数据变化时写 DOM/CanvasTexture。
- 地图使用视锥剔除、HLOD、实例化、遮挡近似、区块加载。
- AI、动画、网络、音频和粒子全部按距离分级。
- Rapier WASM 延迟加载；菜单不阻塞下载物理包。服务器代码从前端构建彻底隔离。
- 删除/重写当前不安全的 `PerformanceOptimizer.applyLOD/applyInstancing`。

#### P1

- 可选 Web Worker：路径规划、地图预处理、压缩解码；不要盲目把强耦合物理迁移到 Worker。
- 探索 WebGPU 渲染器仅作为实验分支，不阻塞 WebGL2 主线。

### 建议帧预算（中画质 60 FPS）

| 模块 | 平均预算 | P95 上限 |
|---|---:|---:|
| 玩家/战斗模拟 | 1.0 ms | 2.0 ms |
| AI | 2.5 ms | 5.0 ms |
| 物理 | 2.0 ms | 4.0 ms |
| 动画 | 1.5 ms | 3.0 ms |
| 网络处理 | 0.7 ms | 1.5 ms |
| UI | 0.7 ms | 1.5 ms |
| 渲染提交 + GPU | 7.0 ms | 12.0 ms |
| 余量 | 1.3 ms | — |

### 验收标准

- 目标机 1080p 中画质 16v16：平均 ≥ 60 FPS，P95 ≤ 16.7 ms，P99 ≤ 25 ms。
- 30 分钟战局 JS heap 和 GPU 资源无持续单向增长。
- GC 单次暂停 P95 < 4 ms。
- 自动画质降级不频繁抖动，恢复有滞回区间。

### 执行记录（2026-08-05）

- [x] **第一批：CPU 帧预算采集与预算数据真实化（“有预算、有数据”）**：审查发现两块真实问题——① `PerformanceBudgetManager.updateStats` 用「纹理数量 × 4 字节」冒充 MB（three.js 不暴露纹理字节数，该指标是虚构数据，有预算无数据）；② `PerformanceOptimizer`/`PerformanceBudgetManager` 全程无调用方（死壳），计划要“删除/重写”的 applyLOD/applyInstancing 早已不在代码里。① 新增 `src/performance/FrameBudget.ts`：CPU 各阶段耗时采集器，按建议帧预算表 7 段（玩家/战斗模拟 1.0/2.0、AI 2.5/5.0、物理 2.0/4.0、动画 1.5/3.0、网络 0.7/1.5、UI 0.7/1.5、渲染提交 7.0/12.0，平均/P95 上限 ms）对齐 `FRAME_BUDGET_TABLE`；begin/end 墙钟测量、滚动窗口（默认 300 帧）统计 last/avg/P50/P95/max/超预算计数、时钟可注入、begin 覆盖未 end 起点防御漏配对、纯逻辑可单测；② GameScene 主循环互斥分段埋点：simulateFixedStep 按顺序切 simulation→ai→simulation→network→ui→physics，animate 包 render 段；PerformancePanel 展示各段 avg/P95/budget 与超预算 ▲ 标记；③ `PerformanceBudgetManager` 修复：移除虚构 textureMemoryMB，改真实计数指标（textures/geometries 上限 512/1024）+ 纯数据入口 setRendererStats（可测试），checkBudget 按真实数据检出超限；④ `PerformanceOptimizer` 持有 frameBudget，getStats 输出全段统计。测试 +11：FrameBudget 6 项（预算表对齐/分段累计/窗口滚动 P50/P95/漏配对防御/allStats 与预算判定/reset）、PerformanceBudget 5 项。门禁 73 文件 / 392 项全绿。
- [x] **第二批：小地图/低频 UI 降频与热路径分配削减**：阶段 9 P0「避免每帧分配 + 低频 UI 只在数据变化时写 DOM/Canvas」首批落地。审查定位三处每帧浪费：① HUD.updateMinimap 每帧全量重绘小地图 Canvas（60Hz 重写 180×180 画布）；② GameScene 每帧 bots.filter().map() 构造小地图敌人数组（20+ 对象 + 数组分配）；③ GameScene 每帧 vehicles.filter().map() 构造地雷触发目标数组。① HUD 小地图降频：新增 `shouldRedrawMinimap` 纯函数（可单测），Canvas 重绘降为 10Hz（`MINIMAP_REFRESH_MS=100`），初始（lastDraw≤0）立即重绘避免首帧空白——测试暴露初始边界（lastDraw=0 且 currentTime=0 时 0≥100 为假），修正为「未绘制过则立即重绘」；② GameScene 小地图敌人数组降频为 200ms 一次（与 HUD 10Hz 重绘解耦）；③ 地雷触发目标改预分配复用数组（mineTargets，每帧 length=0 原地填充，消除 filter/map 闭包与数组分配）。测试 +4（shouldRedrawMinimap 初始/边界/常量/自定义间隔）。门禁 74 文件 / 396 项全绿。
- [x] **第三批：Rapier WASM 延迟加载（菜单不阻塞下载物理包）**：生产代码静态 `import RAPIER from '@dimforge/rapier3d-compat'` 把 rapier JS 包装层（2.2MB min / 830KB gzip）打进主 bundle，WASM 初始化与游戏启动耦合。新增 `src/physics/PhysicsLoader.ts`：动态 `import()` 让 vite 把 rapier 拆成独立 chunk（首屏只加载 index+three）；`ensureRapierLoaded()` 首次进入游戏世界时才拉取模块并 init WASM；promise 单例并发去重；失败自动重置可重试；loader 可注入（测试用 fake 模块，不依赖真实 WASM）。PhysicsWorld/CollisionSystem/VehicleSystem 改类型导入（编译期擦除）+ 运行时经 PhysicsLoader 取模块；PhysicsWorld.init() 走 ensureRapierLoaded；各实例构造时缓存 RapierModule 引用。测试统一入口：PhysicsWorld.test / VehicleSystem.test 的 beforeAll 改 `await ensureRapierLoaded()`。验证：`vite build` 产物 rapier 独立 chunk 2,237KB，主入口 index.js 238KB（gzip 70KB）——首屏不加载物理包证据确凿；测试 +5（PhysicsLoader：未加载抛错/去重 init 一次/并发共享 promise/缓存复用/失败重置可重试）；src/physics + src/vehicle 25 项全绿。注：全量 check 中 server/NetPlay.test.ts ackSeq 用例失败，git diff 确认未触碰 server/，为后台 soak（32 玩家 30Hz）CPU 争抢拉长测试定时器导致的环境性时序失败（发送间隔 > ack 往返），soak 结束后复跑确认。
- [x] **第四批：AI 血条 CanvasTexture 重绘去重 + 性能导出报告补帧预算**：① AI 血条降频（计划 P0「AI 血条降频；只在数据变化时写 CanvasTexture」）：updateAIHealthBars 原先对 3 秒受伤窗口内每个 bot 每帧 clearRect + fillRect×2 + needsUpdate 上传 GPU，但血量只在受击瞬间变化，两次受击之间重绘内容完全相同。改为按 bot 记录上次绘制血量（aiHealthBarDrawnHealth），血量未变不重绘 canvas；透明度淡出改为每帧廉价属性赋值（不触发纹理上传），保持 3 秒淡出平滑性不变。② 性能导出报告补帧预算（第一批遗留缺口）：PerformancePanel 导出 JSON 原先只含 monitor 报告（FPS/帧时间/渲染统计），帧预算数据只在面板显示、不随导出落盘——「有预算、有数据」不完整。update 时缓存最近帧预算，exportReport 合并为 `{ ...report, frameBudget }` 输出。验证：typecheck 通过；src/performance + src/ui + src/game 40 项全绿。
- [x] **第五批：HUD 只在数据变化时写 DOM（值缓存 + 写入计数）**：计划 P0「小地图、计分板、AI 血条、据点 UI 降频；只在数据变化时写 DOM/CanvasTexture」的 DOM 侧落地。HUD.update 原先每帧无条件写 9 处 DOM（血量条宽度/血量文本/弹药文本+低弹量变色/低弹量提示/武器名/K-D 计分/换弹条显隐/换弹进度），多数值只在事件时变化（受击、开火、击杀），帧间写入内容完全相同，白费字符串分配与 DOM 写入。① 值缓存：文本类（healthInt/healthBarWidth/ammoText/weaponName/scoreText）与布尔状态类（低弹变色/低弹提示/换弹条显隐）只在值变化时写 DOM；换弹进度条进度连续变化时照常写。布尔缓存用 null 哨兵表示未初始化——测试暴露了初始 false 与首帧状态相同的边界（首帧不写默认颜色），修正后首帧强制写。② domWriteCount 计数器：每次实际 DOM 写入 +1，既作测试断言点，也作为阶段 9「每帧 DOM 写入」可观测指标。测试：原 4 项（小地图降频）+ 新增 5 项（值变化正确写入/数据未变零写入/血量与弹药独立缓存/低弹变色翻转才写/换弹条状态缓存）。期间发现 vitest vi.spyOn(Element,'textContent','set') 在 jsdom 上失效（setter 不记录、getter 返回 undefined），改用 domWriteCount 行为断言。ui+performance 26 项全绿（cmd 下 exit 0）。
- [x] **第六批：长任务/GC 大暂停观测（LongTaskMonitor）**：验收标准「GC 单次暂停 P95 < 4 ms」需要可观测指标，但浏览器不暴露 GC 事件。新增 LongTaskMonitor：PerformanceObserver('longtask') 监听主线程 >50ms 长任务（GC 大暂停/布局风暴/同步解码的标准代理），60s 滚动窗口统计 count/max/last；observer 工厂与时钟可注入（jsdom/Node 无 PerformanceObserver 时 start() 为 no-op 不抛错）；内存护栏上限 1024 条。接入：GameScene.initializeGameWorld 启动、dispose 停止；F3 性能面板新增 LONG 行（次数/max/last）；导出 JSON 合并 longTask 统计——与 FrameBudget 一起构成「GC/CPU 预算」完整落盘数据。测试 +6：start 创建并 observe/重复 start 去重/阈值过滤与统计/滚动窗口剔除/不支持环境 no-op/reset。typecheck 通过；performance+ui 32 项全绿。
- [x] **第七批：HUD 据点 UI（兵力值/控制点）只在数据变化时写 DOM**：第五批 DOM 值缓存的收尾——征服模式 UI 原先是每次 update 无条件写兵力文本 ×2 与据点样式 ×3（controlPoints 数组每帧重建对象）。① 兵力值缓存：axisTickets/alliesTickets 各自 last 缓存，值未变不写 textContent；② 控制点缓存：按 `id:owner:progress` 序列化 key（progress.toFixed(2) 防浮点抖动），key 未变跳过整块据点样式重绘；③ 均计入 domWriteCount。测试 +1（据点 UI：首帧写入/数据未变零写入/兵力变化只写兵力/归属变化重绘据点），期间发现 fixture id 大小写问题（HUD 元素为 `#cp-A`，测试误写 `#cp-a`）与 jsdom 背景色规范化（`rgba(255, 68, 68, 0.6)` 带空格）两个测试侧修正。ui 6 项全绿；typecheck 通过；全量 409 项仅 server/NetPlay.test.ts ackSeq 时序用例 flaky（负载敏感，单跑 7/7 全绿，与本次改动无关）。
- [x] **第八批：柏林废墟装饰物实例化（碎片/废墟 150 draw call → 2）**：计划 P0「地图使用视锥剔除、HLOD、实例化、遮挡近似、区块加载」的实例化首刀。审查发现 BerlinRuins 装饰物全为独立 Mesh：碎片 100 个（共享 DodecahedronGeometry+材质，纯装饰）+ 废墟 50 个（随机尺寸/旋转 Box），合计 150 个独立 draw call，占渲染提交预算大头。① 碎片 → 单个 InstancedMesh（100 实例，随机位置/旋转，castShadow 保留）；② 废墟 → 单个 InstancedMesh（单位立方体 + per-instance Matrix4 组合 缩放/旋转/位移，随机调用顺序与原实现一致，同一种子下地图布局不变）；③ **碰撞保留**：废墟原在 collisionObjects（弹道射线 1592 / AI 视线 / 玩家开火 Raycast.cast 共用），InstancedMesh 原生支持 Raycaster 命中（返回 world 坐标 hit.point），直接以 InstancedMesh 替换原 mesh 加入 collisionObjects——审查确认三处射线只消费 hit.point/normal/distance，processHit 仅特判载具（严格 === 比较）与 destructibleId（userData），无独立 Mesh 假设；关键坑：InstancedMesh raycast 依赖 boundingSphere，必须 computeBoundingSphere() 否则永不命中（测试覆盖）。收益：地图渲染提交 draw call 减 150（仅装饰部分），阴影实例合并。测试 +4（实例化后独立装饰 Mesh 归零=83/废墟实例可被射线命中且命中点 y 与实例位置一致/collisionObjects 含废墟实例/dispose 移除全部 InstancedMesh）。typecheck 通过；maps+game 21 项全绿；全量 77 文件 / 413 项全绿；vite build 通过（主入口 242KB 不变，rapier 独立 chunk 结构不变）。
- [x] **第九批：柏林废墟建筑/窗户实例化（20+60 draw call → 2）**：实例化第二刀。建筑 20 个（随机尺寸/独立配色）+ 窗户 60 个（共享材质）原为独立 Mesh，与第八批同样的碰撞保留方案。① 建筑 → InstancedMesh（单位立方体 + per-instance 缩放还原尺寸，**instanceColor 还原 5 色随机配色**——Box 颜色不再各自建材质，共享 1 个材质 + per-instance 颜色，setColorAt 后需 instanceColor.needsUpdate）；② 窗户 → InstancedMesh（60 实例，共享 emissive 材质，位置/随机调用顺序与原实现一致，贴在建筑 z+ 面）；③ 建筑本体原在 collisionObjects，实例化后以 InstancedMesh 顶替（raycast 命中实心盒与原一致）；窗户不参与碰撞（原实现即如此）。至此 BerlinRuins 静态场景 draw call：地面1 + 街道2 + 建筑1 + 窗户1 + 碎片1 + 废墟1 = **7 个 draw call**（原 173 个），独立 Mesh 仅剩地面与街道。测试 +1（per-instance 颜色全部落在 5 色集合内）并强化原用例（独立 Mesh 归零=3/建筑实例射线命中/collisionObjects 含建筑与废墟/dispose 清空）。typecheck 通过；maps+game 22 项全绿；全量 77 文件 / 414 项全绿。
- [x] **第十批：静态遮挡地图确定性化（shared 同源布局，服务端建筑挡弹裁决）**：阶段 8 遗留项落地——此前不可破坏建筑仅客户端随机生成（gameplayRandom），服务端无确定性几何可裁决，**子弹穿楼、命中裁决与视觉不一致**。① 新增 `shared/mapLayout.ts`（唯一同源）：mulberry32 固定种子（BERLIN_LAYOUT_SEED=20260805）确定性生成 20 建筑布局（位置/尺寸/配色索引/窗户，随机调用顺序与客户端原实现一致），`layoutToStaticObstacles` 转 ProjectileObstacle 形状（rotationY=0 轴对齐矩形 + 垂直范围，id 基址 10000+ 与可破坏物 0-7 隔离，destroyed 恒 false）；② 客户端 BerlinRuins.createBuildings 改用布局（不再 gameplayRandom，同一种子下地图固定——对战地图固定合理）；③ 服务端 ServerApp 把静态建筑并入每房间 obstacles（与 destructibles 一起挡弹），onObstacleHit 回调按 id ≥ STATIC_OBSTACLE_ID_BASE 跳过扣血（不可破坏只消散）；④ ServerAppOptions 新增 staticLayout 注入（测试传 [] 关闭静态挡弹，保持既有精确战斗/破坏用例不受建筑拦截干扰）。关键验证：全量集成测试首跑 5 失败——排查确认是**真实回归**（测试弹道原本穿楼现在被建筑拦截，非 flaky），注入方案修复后全绿；期间 Set-Content 误伤 5 个测试文件编码（中文注释乱码），git checkout 恢复后 edit_file 精确改回。测试 +7：mapLayout 5 项（同种子确定性/异种子不同/尺寸窗户约束/边界内/障碍物映射）+ ProjectileSim 1 项（静态建筑拦截弹道、目标不命中、弹丸消散）。typecheck（前端+server）通过；全量 78 文件 / 420 项全绿；vite build 通过。
- [x] **第十一批：移除 PerformanceOptimizer 死壳（阶段 9 P0「删除/重写不安全的 PerformanceOptimizer」彻底收尾）**：第一批已删 applyLOD/applyInstancing 并修复其内部虚构数据，但整个类残留且**全程无实例**（grep 仅导出与定义两处、无任何 new、无测试引用），optimizeScene/getOptimizationSettings 同样无调用方——是第一批「死壳」诊断的残留。直接删除 `src/performance/PerformanceOptimizer.ts` + 移除 index.ts 导出（frameBudget/dynamicResolution/PerformanceMonitor 等由 GameScene 直接持有，不受影响）。验证：typecheck 通过；performance 26 项全绿。

### 阶段 9 验收小结（P0 全部落地）

对照 P0 任务清单与验收标准逐项核对：

| P0 任务 | 落地证据 |
|---|---|
| 建立 CPU/GPU/GC 分析流程 | 一批 FrameBudget（7 段预算）+ 六批 LongTaskMonitor（GC 大暂停代理）+ 四批导出报告合并帧预算 |
| 拆解帧预算 | 一批按建议表 7 段对齐，F3 面板展示 avg/P95/超预算 ▲ |
| 避免每帧分配 | 二批（小地图数组/地雷目标预分配复用）+ 五/七批（DOM 值缓存）+ 九批（instanceColor 免材质） |
| 低频 UI 只在数据变化时写 DOM/Canvas | 二批小地图 10Hz、四批 AI 血条去重、五/七批 HUD 值缓存 + domWriteCount 可观测 |
| 地图视锥剔除/HLOD/实例化/遮挡近似/区块加载 | **实例化**（八/九批：静态场景 173→7 draw call）；视锥剔除 three.js 默认开启（实例化后 computeBoundingSphere 已保证 boundingSphere 正确）；HLOD/区块加载/遮挡近似在 120×120 小地图 + 7 draw call 下收益有限，留待地图扩展 |
| AI/动画/网络/音频/粒子按距离分级 | AI：AILod（感知 100ms-1s/决策 200ms-1.5s/visible 分级，已有）；动画：程序化动画无 mixer，mesh.visible 随 LOD；网络：interest management（computeVisiblePlayers）；音频：AudioVoiceManager 距离虚拟化；粒子：VfxPool 距离 LOD |
| Rapier WASM 延迟加载 | 三批：独立 chunk 2,237KB，主入口 238KB（gzip 70KB） |
| 服务器代码与前端构建隔离 | vite build 不碰 server；server 运行时仅依赖 shared/（src/network 仅测试与压测客户端引用） |
| 删除/重写不安全 PerformanceOptimizer | 一批删 applyLOD/applyInstancing，十一批整体移除死壳类 |
| 静态遮挡确定性化（阶段 8 遗留） | 十批：shared/mapLayout 同源，服务端建筑挡弹，子弹不再穿楼 |

对照验收标准：

1. **1080p 中画质 16v16 平均 ≥60 FPS / P95 ≤16.7ms / P99 ≤25ms**：代码侧预算表对齐 + draw call 173→7 + DynamicResolution 自动降级已具备；目标机浏览器实测待真机验证（F3 面板 + 导出 JSON 可直接取证）。
2. **30 分钟战局 JS heap / GPU 无持续单向增长**：服务端 2 小时 soak 通过（32 玩家 RSS 稳定 110-129MB、errors=0）；客户端分配削减落地，浏览器 heap 观测待真机。
3. **GC 单次暂停 P95 < 4ms**：LongTaskMonitor 已可观测（F3 LONG 行 + 导出合并），浏览器实测待真机。
4. **自动画质降级不频繁抖动、恢复有滞回**：DynamicResolution hysteresisMs=3 + minIntervalMs=500 + 死区 toleranceMs=2，测试覆盖（含临界抖动用例），代码满足。

结论：**阶段 9 P0 全部落地**；1-3 项为浏览器真机验证项（观测工具已就绪，无代码缺口），第 4 项代码+测试满足。

---

## 阶段 10：UI/UX、可访问性与发布准备

**目标：** 做成完整产品流程，而不是开发演示。  
**建议周期：** 2 周。

### 执行记录（2026-08-06）

- [x] **第一批：设置持久化扩展 v2（音量分组/分辨率比例/减少震动 + v1 迁移）**：P0「设置持久化：画质、分辨率比例、FOV、灵敏度、ADS 倍率、音量分组、键位」落地。① GameSettings 扩展：单一 volume 拆为 volumeMaster/volumeSfx/volumeMusic/volumeVoice 四组；新增 resolutionScale（0.5-1.5，乘到像素比，与 DynamicResolution 协同）；新增 reduceScreenShake（可访问性）；STORAGE_KEY v1→v2，`migrateV1Settings` 把旧 volume 字段并入 volumeMaster（其余分组取默认，旧存档无缝升级，sanitize 链自动走迁移）；② AudioSystem：SoundConfig 增加 category（sfx/music/voice），SOUND_CONFIGS 归类（战斗音效→sfx、AMBIENT→music、UI 提示音→voice），新增 setGroupVolume/getGroupVolume（钳制 0-1），play() 增益乘分组音量，setVolume 保留兼容；③ PlayerController：shakeMultiplier（reduceScreenShake 时 0.3），addShake 幅度乘之；④ GameScene.applySettings：应用四组音量 + basePixelRatio 乘 resolutionScale（低画质仍固定 1）；⑤ SettingsMenu：字段同步 + 新增分组音量滑块×3、渲染分辨率比例滑块（50-150%）、减少震动复选框，界面分组（音量/操作/画面/可访问性）。测试：GameSettings +5（v1 迁移到 volumeMaster/旧存档自动迁移/新字段默认与钳制/分组音量 sanitize 边界）、AudioSystem +4（分组映射/音乐与 UI 归类/分组独立钳制/与主音量互不影响）——AudioSystem 此前无测试文件，新建。验证：typecheck 通过；config+audio+ui+player 43 项全绿；vite build 通过；全量 79 文件仅 CombatLoop 回溯用例 flaky（真实定时器负载敏感，单跑 4/4 全绿，与本次改动无关）。
- [x] **第二批：键位重绑定系统（KeyBindings 模块 + InputManager 重绑定 + 持久化 + 冲突检测）**：P0「操作可访问性：键位重绑定」落地。① 新增 `src/input/KeyBindings.ts`：19 个动作（移动/跳跃/冲刺/蹲伏/换弹/武器×4/装备/投掷/载具/座位/天气/计分板/设置菜单）的 `KeyActionId` 全集 + `DEFAULT_KEY_BINDINGS`（WASD/空格/Shift/ctrl/R/数字键/Q/G/E/F/T/Tab/Esc，与 InputManager 原有默认一致）；绑定用 KeyboardEvent.code（物理键位，不受键盘布局影响）；`loadKeyBindings/saveKeyBindings` 持久化到 `zhandi.key-bindings.v1`（JSON + sanitize 链：缺省动作补默认、非法值回退，损坏存档静默回默认）；`findKeyConflicts` 冲突检测（同一键绑多动作 → 返回 code→动作列表，靠后动作不触发）；`buildCodeToActions` 反向映射（code→动作列表）；`KEY_ACTION_LABELS` 中文显示名。② InputManager 重构支持重绑定：持键状态 keyDown/keyPressed 的判定从硬编码 code 改为「当前动作集合」查询——`bindings` 字段 + `setBindings()`，每次重绑定重建 code→actions 反向映射；`getActionsForCode` 供设置界面预览；`isActionDown/isActionPressed` 内部按动作查键（支持一个动作多键、一个键多动作）。③ GameScene：startGame 时 `inputManager.setBindings(loadKeyBindings())` 接入持久化；设置菜单新增「键位」区（动作列表 + 当前键显示 + 点击录制 + 冲突红色提示 + 恢复默认）。测试：KeyBindings +4（默认完整/持久化往返/sanitize 非法值回退/冲突检测）、InputManager 重绑定 +2（重绑定后新键生效旧键失效/一键多动作）。验证：typecheck 通过；input+ui 相关全绿；vite build 通过；全量 79 文件 / 420 项全绿（CombatLoop 回溯用例 flaky 修复已单独提交，重试机制下更稳）。
- [x] **第三批：可访问性设置（色觉模式阵营配色 + 准星样式/颜色/大小）**：P0「操作可访问性：色觉模式、关闭动态模糊、准星设置」落地（动态模糊项目本无后处理，该项自然满足）。① 新增 `src/ui/teamColors.ts`：4 种色觉模式（正常/绿色弱 deuteranopia/红色弱 protanopia/蓝黄色弱 tritanopia）各一套红蓝阵营高对比配色（绿弱下德军红→橙 `#ff8800`、蓝弱下苏军蓝→青绿 `#22c8a8`），`getTeamColors` 非法回退正常、`hexToRgba` 半透明背景用；② GameSettings 扩展：`colorBlindMode` / `crosshairStyle`（四段/点/无）/ `crosshairColor`（hex 校验）/ `crosshairScale`（0.5-2），sanitize 非法枚举/颜色/范围回退默认；③ HUD：`applyAccessibility()` 统一应用——色觉模式切据点容器/标签/数字/控制点（按缓存的 lastCpOwners 即时重绘，不依赖下次 update）+ 小地图敌人/友军色，清空据点 key 缓存与强制小地图重绘；准星新增中心点 `#cross-dot` 元素，样式切换四段/点/无显隐、颜色应用到四段+点（hexToRgba 0.9 半透明）、大小 scale 到容器 transform；④ SettingsMenu 可访问性分组新增色觉模式下拉、准星样式下拉、准星颜色取色器、准星大小滑块；⑤ GameScene.applySettings 接入。测试：teamColors +6（各模式配色/非法回退/hexToRgba）、GameSettings +1（可访问性字段默认+非法回退）、HUD +2（色觉切换即时重绘据点/准星三设置）。验证：typecheck（前端+server）通过；ui+config 21 项全绿；vite build 通过；全量 81 文件 / 448 项全绿（1 次 NetPlay 时序 flaky，复跑通过）。
- [x] **第四批：断线/重连状态覆盖层（顶部横幅 + 重试/返回主菜单可恢复操作）**：P0「断线提示、重连状态、服务器错误均提供可恢复操作」落地。① 新增 `src/ui/ConnectionStatusOverlay.ts`：顶部悬浮横幅组件——`showReconnecting(attempt, delayMs)` 黄色非阻塞提示（“连接中断，正在重连…第 N 次，Xs 后重试”，无按钮）、`showConnected()` 隐藏、`showFailed(onRetry, onBackToMenu)` 红色失败横幅 + 「重新连接/返回主菜单」按钮（点击触发回调）、`dispose()` 移除 DOM 与回调；② NetClient 新增回调：`onReconnectScheduled(attempt, delayMs)` 在 scheduleReconnect 指数退避时触发（UI 显示重连中）、`onReconnectSuccess` 在 hello_ack 且 reconnectAttempts>0（确为重连而非首次连接）时触发，重连计数复位；③ NetworkGameClient 透传两个回调；④ GameScene.connectToServer 接入覆盖层（断线→重连中横幅、重连成功→隐藏、max_reconnects→失败横幅+可恢复操作），新增 `exitToMainMenu()`：dispose 覆盖层 → disconnect 网络 → 清 serverGameState/clientPrediction → 状态机回 MENU → 显示主菜单；`dispose()` 补覆盖层清理。测试：ConnectionStatusOverlay +4（重连提示文案/隐藏/失败横幅按钮点击回调/dispose 清理）、NetClient +1（断线调度触发 onReconnectScheduled、重连成功触发 onReconnectSuccess 且计数复位）。验证：typecheck 通过；eslint 相关文件通过（修复 hello_ack case 块 no-case-declarations）；net+ui 20 项全绿；全量 82 文件 / 453 项全绿。
- [x] **第五批：字幕系统（SubtitleOverlay + 据点易主播报 + showSubtitles 开关）**：P0「操作可访问性：字幕」与验收标准「关键音频信息有视觉替代」落地。① 新增 `src/ui/SubtitleOverlay.ts`：屏幕底部中央字幕层——`show(text, durationMs?)` 新字幕替换旧字幕并重置计时（默认 3s）、淡入淡出（opacity 过渡 + 淡出后隐藏容器）、`setEnabled()` 开关（关闭立即隐藏并丢弃后续字幕）、`isVisible()`/`dispose()`；z-index 190（低于断线覆盖层 200，互不遮挡）；② ConquestMode 新增 `onControlPointCaptured(pointId, owner, name)` 回调，占领完成时触发（未完成不触发）；③ GameScene：init 创建字幕层；`ui:message` 事件（回合阶段/击杀/被击杀/胜负等全部播报性质消息）同步走底部字幕；据点易主回调 → 「「名称」被德军/苏军占领」3.5s 字幕；`applySettings` 接入 showSubtitles 开关；`dispose()` 清理；④ GameSettings 新增 `showSubtitles`（默认 true，sanitize 缺省补默认、显式 false 保留，旧存档自动开）；⑤ SettingsMenu 可访问性分组新增「显示字幕」复选框（含 sync/read）。测试：SubtitleOverlay +6（显示/替换重置计时/超时隐藏/关闭丢弃后续/重新开启恢复/dispose）、ConquestMode +2（占领完成触发回调含参数/未完成不触发）、GameSettings +1（字幕默认开+缺省补默认+显式 false 保留，sanitize 全量比对同步）。验证：typecheck 通过；eslint 相关文件 0 error（1 个既有 dt 未用 warning）；ui+config+game 21 项全绿；vite build 通过；全量 83 文件 / 462 项全绿。

### 任务

#### P0

- 统一原创 UI 视觉系统：字体、色板、边框、图标、动效、间距、焦点状态。
- 完整流程：首次设置 → 主菜单 → 服务器/模式 → 小队与兵种 → 部署地图 → 战局 → 结算。
- 设置持久化：画质、分辨率比例、FOV、灵敏度、ADS 倍率、音量分组、键位。
- 操作可访问性：键位重绑定、色觉模式、字幕、减少屏幕震动、关闭动态模糊、准星设置。
- 加载进度、断线提示、重连状态、服务器错误均提供可恢复操作。
- 增加崩溃/错误匿名统计的用户同意机制。

#### P1

- 新手训练场、兵种引导、载具教程。
- 回放/观战与举报入口。

### 验收标准

- 新用户不看文档可完成：进入战局、选择兵种、部署、占点、补给/救援、退出。
- 所有设置重启后保持，恢复默认可用。
- 720p–4K、16:9/16:10/21:9 下核心 UI 不遮挡。
- 键盘全程可导航菜单，关键音频信息有视觉替代。

---

## 阶段 11：Alpha、Beta 与上线后闭环

**目标：** 通过真实玩家数据收敛平衡、稳定性和体验问题。  
**建议周期：** 持续进行。

### Alpha（功能完整）

- 冻结核心系统范围，只修 P0/P1 缺陷。
- 进行 10–20 人封闭测试，记录崩溃、网络、帧时间、出生死亡、武器命中和据点热力图。
- 每局结束收集 3 个问题：最不清楚、最挫败、最有趣的部分。

### Beta（内容与平衡）

- 进行 16v16 定期测试。
- 通过遥测调整武器 TTK、兵种选取率、地图热点、载具生存时间和局长。
- 只允许配置平衡改动，避免 Beta 后期重构底层架构。

### Release Candidate

- 2 小时服务器 soak、30 分钟客户端内存测试、多浏览器冒烟、断网恢复、版本兼容测试全部通过。
- 法务资源清单 100% 完成。
- 部署包含回滚、协议版本兼容、静态资源缓存失效策略。

### 上线后核心指标

| 指标 | 目标/观察方向 |
|---|---|
| 客户端崩溃率 | < 0.5% 会话 |
| 服务器异常结束率 | < 0.2% 战局 |
| 中画质性能 | 目标机 P95 ≤ 16.7 ms |
| 网络 | 100 ms RTT 下无持续橡皮筋 |
| 单局时长 | 12–20 分钟 |
| 出生 3 秒内死亡率 | < 5% |
| 兵种选取 | 任一兵种长期不超过 45% |
| 载具平均寿命 | 既不秒毁，也不统治整局；以测试数据校准 |

---

# 5. 工作流与交付规范

## 5.1 每个功能的 Definition of Done

一个功能只有同时满足以下条件才算完成：

- 玩法逻辑实现，并具有单元/集成测试或可自动回放验证。
- 联网功能明确客户端和服务器权威边界。
- 有调试信息和错误处理。
- 有性能预算，不引入不可解释的 P95 退化。
- 支持 `dispose()` 或生命周期回收。
- 设置项、输入、UI 和音频按需接入。
- 文档和配置说明同步更新。

## 5.2 分支与发布策略

- `main`：始终可构建、可运行。
- 功能分支：一项功能一个分支，禁止把架构重构和大量内容改动混在同一提交。
- 每阶段建立里程碑标签，例如 `m1-foundation`、`m2-infantry-slice`、`m3-conquest`。
- 协议变更必须提升 `protocolVersion`，必要时提供一个版本的兼容窗口。

## 5.3 必备调试工具

- 自由相机/观战相机。
- 实体、碰撞体、导航网格、AI 感知、射线和伤害可视化。
- 固定随机种子与输入录制/回放。
- 网络延迟/抖动/丢包模拟。
- 快速跳转据点、生成 Bot/载具、切换天气、给予装备。
- 性能 HUD 与一键导出采样。

---

# 6. 建议的首个 30 天执行清单

## 第 1 周：基线与安全改造

- [ ] 增加 `typecheck/test/build` 门禁。
- [ ] 固定基准战场和随机种子。
- [ ] 加入性能 HUD 和 15 分钟基线记录。
- [ ] 修复物理固定步长、失焦时间跳变、重复启动问题。
- [ ] 标记 README 中“已接入/未接入”的真实状态。

## 第 2 周：拆分主循环

- [ ] 建立游戏状态机。
- [ ] 抽离 `Simulation`、`CombatSystem`、`UISystem`。
- [ ] 建立事件总线和统一时钟。
- [ ] 完成战斗事件、占点事件、击杀统计迁移。

## 第 3 周：移动与 STG44 精修

- [ ] Shape cast 地面检测与台阶处理。
- [ ] 接入灵敏度、ADS 倍率和设置持久化。
- [ ] STG44 后坐力曲线、距离衰减、弹道与换弹事件。
- [ ] 建立靶场自动采样。

## 第 4 周：征服垂直切片

- [ ] 统一阵营和计分数据源。
- [ ] 完整回合状态与结算。
- [ ] 安全出生点选择。
- [ ] 4 兵种最小原型与部署界面线框。
- [ ] 组织一次 5–8 人可玩性测试，形成问题优先级列表。

### 30 天验收目标

- 架构可继续扩展，核心系统可测试。
- 一张灰盒地图、一种征服模式、一个精修兵种、两把有差异的武器形成稳定垂直切片。
- 30/60/120 FPS 下移动与射速基本一致。
- 中端设备 1080p 基准场景 P95 帧时间有可追踪数据，不出现持续内存增长。

---

# 7. 风险清单与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| 同时追求 3A 画质、64 人、全破坏 | 范围失控，长期不可交付 | 锁定 16v16 垂直切片，阶段验收后再扩 |
| 先堆模型特效再修架构 | 返工和性能债快速累积 | 阶段 0–2 优先，资产只做验证样例 |
| 客户端权威继续扩展 | 作弊、一致性和命中争议无法解决 | 阶段 8 前先定义协议和共享模拟边界 |
| 地图视觉、碰撞、导航三套数据 | 穿模、AI 卡死、破坏不同步 | 同源导出，自动校验覆盖 |
| AI 每帧全量决策 | 32 Bot 时 CPU 爆炸 | 分频、空间索引、AI LOD、事件驱动感知 |
| 粒子和动态灯堆叠 | 爆炸场景严重掉帧 | 全局预算、重要性队列、池化与降级 |
| 未经授权复用商业素材 | 无法发布 | 原创/合法资产清单和许可证门禁 |
| 过早上 WebGPU | 浏览器兼容与开发成本失控 | WebGL2 主线，WebGPU 仅实验分支 |
| README 与真实状态偏差 | 决策错误、重复建设 | 阶段 0 建功能矩阵并持续维护 |

---

# 8. 里程碑总览

| 里程碑 | 对应阶段 | 可交付结果 |
|---|---|---|
| M0 可测原型 | 0–1 | 架构解耦、固定主循环、质量与性能基线 |
| M1 步兵垂直切片 | 2 | 可靠移动、2 把精修武器、靶场数据 |
| M2 征服可玩版 | 3–4 | 4 兵种、小队雏形、可自主作战的 16v16 Bot |
| M3 视听垂直切片 | 5–6 | 原创废墟地图、角色动画、战场渲染与分层音频 |
| M4 战场沙盒版 | 7 | 坦克、运输、反载具和局部破坏闭环 |
| M5 联机 Alpha | 8–9 | 权威服务器、16v16 稳定同步、性能达标 |
| M6 发布候选 | 10–11 | 完整产品流程、可访问性、遥测、压测与回滚 |

## 最终建议

当前最重要的不是继续增加“功能名称”，而是把已有功能做成真实闭环。建议立即从 **阶段 0 → 阶段 1 → 阶段 2** 开始，前三个阶段完成前，不新增地图、天气类型或武器数量。只有当固定步长、架构拆分、自动测试和两把精修武器全部达标后，后续 AI、载具、画面和联网投入才不会反复返工。
